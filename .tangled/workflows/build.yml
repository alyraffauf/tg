when:
  - event: ["push", "manual"]
    branch: ["master"]
  - event: ["pull_request"]
    branch: ["master"]

engine: "nixery"

dependencies:
  nixpkgs:
    - nix

steps:
  - name: "Build tg"
    command: "nix build .#tg"
